apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}
  labels: {{ include "rhsso.labels" $ | nindent 4 }}
spec:
  ports:
  - name: http
    port: 80
    targetPort: 8080
  selector:
    deployment: {{ .Release.Name }}
