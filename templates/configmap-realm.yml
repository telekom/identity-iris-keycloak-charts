---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-realm
  labels: {{ include "keycloak.labels" $ | nindent 4 }}
data:
{{ ( tpl (.Files.Glob "realms/*.json").AsConfig  . ) | indent 2 }}

