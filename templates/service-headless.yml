apiVersion: v1
kind: Service
metadata:
  name: {{ include "keycloak.fullname" . }}-jgroups
  labels: {{ include "keycloak.labels" $ | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  selector: {{ include "keycloak.selector" $ | nindent 4 }}